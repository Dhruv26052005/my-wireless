<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- Exclude sensitive data from cloud backup -->
        <exclude domain="sharedpref" path="mesh_encryption.xml"/>
        <exclude domain="database" path="mesh_database"/>
    </cloud-backup>
    <device-transfer>
        <!-- Exclude sensitive data from device transfer -->
        <exclude domain="sharedpref" path="mesh_encryption.xml"/>
        <exclude domain="database" path="mesh_database"/>
    </device-transfer>
</data-extraction-rules>
